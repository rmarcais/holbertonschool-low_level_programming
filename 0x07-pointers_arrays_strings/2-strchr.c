#include "main.h"
#include <string.h>
/**
 * _strchr - locates a character string
 *@s: points to a character
 *@c: character
 * Return: pointer.
 */
char *_strchr(char *s, char c)
{
	return (strchr(s, c));
}
